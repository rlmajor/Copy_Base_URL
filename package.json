{
  "name": "project-name",
  "version": "1.1",
  "scripts": {
    "test": "jest --coverage"
  },
    "collectCoverage": {
      "coverageDirectory": "coverage",
      "coverageReporters": ["json", "lcov", "text", "clover"]
  },
  "devDependencies": {
    "jest": "^27.0.0"
       }
}
